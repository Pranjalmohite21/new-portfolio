name: Deploy Portfolio to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4   # ✅ update to v4

      - name: Setup Pages
        uses: actions/configure-pages@v4   # ✅ latest version

      - name: Build Site
        run: |
          mkdir -p public
          cp -r * public/
          echo "yourdomain.com" > public/CNAME  # custom domain

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3   # ✅ latest version
        with:
          path: public

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4   # ✅ latest version

      - name: Slack Notification
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_USERNAME: "GitHub Actions"
          SLACK_TITLE: "🚀 GitHub Pages Deployment Completed"
          SLACK_MESSAGE: "Portfolio site deployed successfully!"
